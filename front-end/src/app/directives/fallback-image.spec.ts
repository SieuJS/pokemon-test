import { FallbackImage } from './fallback-image.directive';

describe('FallbackImage', () => {
  it('should create an instance', () => {
    const directive = new FallbackImage();
    expect(directive).toBeTruthy();
  });
});
